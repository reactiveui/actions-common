name: .NET Build (UNO)
description: UNO/msbuild variant build and pack (Windows only)

inputs:
  configuration:
    description: 'Build configuration'
    required: false
    default: 'Release'
  src-folder:
    description: 'Folder containing the solution'
    required: false
    default: 'src'
  solution-file:
    description: 'Solution file relative to src-folder'
    required: false
    default: ''

runs:
  using: composite
  steps:
    # SOLUTION_FILE stays unquoted so an empty value passes no argument.
    - name: Restore NuGet packages
      shell: bash
      env:
        SOLUTION_FILE: ${{ inputs.solution-file }}
      run: dotnet restore $SOLUTION_FILE
      working-directory: ${{ inputs.src-folder }}

    - name: Add MSBuild to PATH (Visual Studio)
      uses: microsoft/setup-msbuild@30375c66a4eea26614e0d39710365f22f8b0af57 # v3
      with:
        vs-prerelease: true

    - name: Build and pack UNO solution with MSBuild
      shell: bash
      env:
        # Git Bash rewrites path-like arguments, mangling the /t: and /p: switches.
        MSYS2_ARG_CONV_EXCL: '*'
        CONFIGURATION: ${{ inputs.configuration }}
        SOLUTION_FILE: ${{ inputs.solution-file }}
      run: msbuild /t:build,pack /nowarn:MSB4011 /maxcpucount /p:NoPackageAnalysis=true /verbosity:minimal "/p:Configuration=$CONFIGURATION" $SOLUTION_FILE
      working-directory: ${{ inputs.src-folder }}
