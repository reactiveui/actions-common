# Trusts reactiveui in the actions/unpinned-tag query: org repositories reference this repo's
# reusable workflows @main on purpose. Actions from other owners are still reported.
extensions:
  - addsTo:
      pack: codeql/actions-all
      extensible: trustedActionsOwnerDataModel
    data:
      - ["reactiveui"]
